name: CI

on:
  pull_request:
  push:
      branches:
      - master

jobs:
  run-tests:
    runs-on: [ubuntu-latest]

    env:
        DEEPSOURCE_DSN: ${{ secrets.DEEPSOURCE_DEV_DSN }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      with:
        fetch-depth: 1
        ref: ${{ github.event.pull_request.head.sha }}

    - name: Set up python3.9
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Set up NodeJS
      uses: actions/setup-node@v3
      with:
        node-version: 16

    - name: Run tests
      run: |
        pip install pytest-cov django
        pytest --cov=./ --cov-report=xml

    - name: Run JS tests
      run: |
        npm install -g yarn
        cd javascript
        yarn install
        yarn test

    - name: Report test coverage to DeepSource
      run: |
        curl https://deepsource.io/cli | sh
        ./bin/deepsource report --analyzer test-coverage --key python --value-file ./coverage.xml
        ./bin/deepsource report --analyzer test-coverage --key javascript --value-file ./javascript/coverage/cobertura-coverage.xml
